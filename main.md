https://github.com/ilyanych186-boop/Python-

- Задание 1

lists = [1, 'Привет', False, 3.14]
создал произвольный список

lists.append('мир')
В конец списка добавляется строка 'мир'

lists.insert(0, 67)
В начало списка (индекс 0) вставляется число 67

list2 = [2, 'World', True]
Создаётся новый список list2 с тремя элементами.

lists.append(list2)
В конец lists добавляется сам список list2 как один элемент.

lists.insert(-1, (12, 5))
Перед последним элементом (индекс −1) вставляется кортеж (12, 5).

print(lists[1])
Выводится элемент с индексом 1 (второй по счёту) — это 1.

lists.pop(5)
Удаляется элемент с индексом 5 (шестой по счёту) — это строка 'мир'.

count = lists.count(1)
Подсчитывается, сколько раз значение 1 встречается в списке. Оно есть один раз (на позиции 1). Переменная count получает значение 1.

- Задание 2

dict1 = {'name': 'Ilya', 67: 'age', 'counter': 'Russia'}
создал произвольный словарь
dict1['rost'] = 176
В словарь добавляется новая пара: ключ 'rost', значение 176.

dict1[(98, 56, 82)] = [1, 3, 52]
В словарь добавляется пара с ключом‑кортежем (98, 56, 82) и значением‑списком [1, 3, 52].

print(dict1['rost'])
Выводится значение по ключу 'rost' — это 176.

del dict1[67]
Удаляется пара с ключом 67

print('словарь', list(dict1.keys()))
Выводится строка 'словарь' и список всех ключей словаря.

- 3 Задание

l = [1, 56, 3254, 4, 7, 6, 5, 89, 12, 139, 76, 77]
Создаётся список чисел.

for i in l:
Цикл перебирает элементы списка по порядку.

if i == 139:
    break
Если текущий элемент равен 139, цикл прерывается

if i % 2 != 0:
    print(i)
Если число нечётное (i % 2 != 0), оно выводится на экран.

- 4 задание

lst = [11, 5, 8, 32, 15, 3, 20, 132, 21, 4, 555, 9, 20]
Создаётся список чисел.

count_lst = 0
Инициализируется переменная для суммы.

for num in lst:
Цикл перебирает элементы списка.

if num < 30 and num % 3 == 0:
Если число меньше 30 И делится на 3 без остатка, оно выводится.

else: count_lst += num
Иначе число добавляется к сумме count_lst.

- 5 Задание

созадл функцию
def month_to_season(month):

seasons = {1:'зима', 2:'Зима', 3:'Весна', 4:'Весна', 5:'весна', 6:'лето', 7:'Лето', 8:'Лето', 9:'Осень', 10:'Осень', 11:'Осень', 12:'Зима'}
внес туда словарь месяцев(я подумал что будет легче сделать это через словарь)

if month in seasons:
проверяется наличия месяца в словаре

    return seasons[month]
else:
    return 'Нету такого номера месяца'
если в месяце от 1 до 12 нет такого, то пишет что "Нету такого номера месяца".

print(month_to_season(14))
выводит сезон который указал вместо 14(я когда проверял код, я забыл поменять 14 на диапозон 1-12)